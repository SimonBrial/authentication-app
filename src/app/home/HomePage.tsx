import HomeContainer from "./HomeContainer";

const HomePage = () => {
  return (
    <section className="w-full m-auto h-screen">
      <HomeContainer />
    </section>
  );
};

export default HomePage;
